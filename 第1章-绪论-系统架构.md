# 第1章 时空信息移动应用系统架构

> [!SUMMARY] Table of Contents
>
> - [[第1章-绪论-系统架构#第1章 时空信息移动应用系统架构|第1章 时空信息移动应用系统架构]]
>   - [[第1章-绪论-系统架构#目录|目录]]
>   - [[第1章-绪论-系统架构#§1 时空信息移动应用多层软件体系结构|§1 时空信息移动应用多层软件体系结构]]
>   - [[第1章-绪论-系统架构#§2 移动端编程技术|§2 移动端编程技术]]
>     - [[第1章-绪论-系统架构#移动端硬件与系统|移动端硬件与系统]]
>     - [[第1章-绪论-系统架构#三种移动开发方式|三种移动开发方式]]
>     - [[第1章-绪论-系统架构#混合编程核心机制|混合编程核心机制]]
>     - [[第1章-绪论-系统架构#嵌入式移动端|嵌入式移动端]]
>   - [[第1章-绪论-系统架构#§3 应用服务端编程技术|§3 应用服务端编程技术]]
>   - [[第1章-绪论-系统架构#§4 时空数据管理|§4 时空数据管理]]
>   - [[第1章-绪论-系统架构#§5 移动数据应用分析|§5 移动数据应用分析]]
>   - [[第1章-绪论-系统架构#应用实例|应用实例]]
>   - [[第1章-绪论-系统架构#本章总结|本章总结]]

---

## §1 时空信息移动应用多层软件体系结构

主要包括如下几个层次：**移动端 → 数据存储端 → 应用服务端 → 应用分析端**。

数据流向：**数据采集 → 存储 → 分析 → 应用**。

各层通过标准服务接口衔接，基于法规和安全标准运行：

- 数据采集端 → 共享服务平台 → 应用（分析）端
- 标准涵盖：数据服务接口标准、时空大数据集服务接口标准、可定制应用服务接口标准等

**典型网络架构**：移动端通过互联网/内部网向应用服务端发起请求，服务端动态页面响应，数据服务端支撑后端数据存取。

> 实例：https://jwgl.whu.edu.cn——思考哪些部分体现移动（客户）层、服务层、应用逻辑层、数据（库）层。

---

## §2 移动端编程技术

### 移动端硬件与系统

多种移动设备（硬件） + 多种操作系统（软件）：

- **操作系统**：Android、iOS、Mac、嵌入式系统
- **移动编程方法**：网页、小程序、公众号、原生、混合式

### 三种移动开发方式

| 方式 | 优点 | 缺点 |
|------|------|------|
| **Native App** | 可访问所有硬件（GPS、摄像头等）；速度快、性能高；支持大量图形动画；兼容性好、防病毒漏洞；设备接口处理快 | 开发周期长（3~5个月）；费用高昂；可移植性差（Android/iOS各自开发）；内容受App Store限制；升级需重新下载 |
| **Web App (H5)** | 跨平台（Android/iOS/Windows）；成本低周期短；无内容限制；适合大段文字展示；自动更新 | 不能直接访问设备硬件和离线存储；高度依赖联网；功能有限；反应速度慢；图片动画支持不佳；无法调用摄像头/麦克风 |
| **Hybrid App** | 开发效率高（一套代码跨平台）；更新升级方便（服务器端升级即可）；维护方便、成本低；比Web版功能多；可离线运行 | 功能/界面固定无法自定义；加载缓慢、网络占用高；安全性较低；高端人才难找（需同时懂原生+H5） |

> 混合开发 = "HTML5云网站 + APP应用客户端"，原生部分提供WebView容器，业务实现和界面展示用H5。典型应用：京东、淘宝、今日头条。

### 混合编程核心机制

- **WebView组件**：Android 用 `webview`，iOS 用 `UIWebview`/`WKWebview`，均可加载HTML文件，依赖 Webkit（渲染+JS引擎）
- **JS引擎**：JavaScriptCore 和 V8，通过 WebView 实现 Native 与 JS 交互
- **Android ↔ JS 交互**：
  - Android 调用 JS：`loadUrl()` / `evaluateJavascript()`
  - JS 调用 Android：`addJavascriptInterface()` 对象映射 / `shouldOverrideUrlLoading()` URL拦截 / `onJsAlert()`、`onJsConfirm()`、`onJsPrompt()` 对话框拦截
- **iOS 端**：通过 JavaScriptCore 完成类似注入机制

**简单示例**（简要）：基于网页的浏览器定位 `show_location.html`；Android Studio / 命令行创建并运行原生应用；原生应用嵌入 WebView 加载 `index.html`。

### 嵌入式移动端

基于单片机处理器，附加不同传感器，通过串口通信，由无线网络接口向服务端传输采集数据。开发语言包括机器语言、汇编语言与高级语言（Python、Arduino等）。

---

## §3 应用服务端编程技术

**作用**：桥接移动端和数据存储端：
- 为移动端数据采集提供数据处理、过滤和封装
- 为移动端应用提供数据分析服务
- 为数据安全提供保护屏障

**功能**：接收移动端数据提交、为移动端提取数据、数据双向过滤封装、移动数据业务处理，通常部署于云服务器。

**实现方式**：独立服务进程，部署于云服务器，开放服务端口，支持多种编程语言（Asp、Jsp、Php、Python 等）。

**连接方式**：
- 持续连接（TCP/IP 模式）
- 一次性连接（HTTP(S) 模式）

**示例**（简要）：
- Python TCP/IP 采集 PM2.5 数据（`Chapter1.1.py`），移动端用 NetAssist 模拟，输入 x,y,时间,PM2.5值
- JSP HTTP 实现数值采集（`Chapter1.2.jsp`）和查询（`Chapter1.3.jsp`），通过 URL 参数传递数据

---

## §4 时空数据管理

移动数据的采集需要**持续化存储**，有两种方式：

- **文件管理**：按规范持续化存储，支持二进制和文本格式
- **数据库管理**：灵活性更大、效率更高、选择多样

常见数据库系统：Oracle、MySQL、MS SQL、PostgreSQL、MongoDB。需熟练掌握 DDL、DML、DCL、DQL 操作及数据库设计。

---

## §5 移动数据应用分析

以时空信息为基本维度进行分析：

- 时间序列变化分析
- 空间格局（差异性）分析
- 空间分布模式（聚集、分散、统计等）
- 时、空关联分析

承载专业信息的分析：交通时空变化模式、PM2.5与工业企业布局关系、气象数据与植被生长关系等。需采用空间相关分析、空间聚类分析等模型挖掘移动数据蕴含的模式。

---

## 应用实例

**社会和资源调查 App**：采集表格手工录入或自动入库 → 数据集存储 → 服务及应用接口层 → 时空数据公共服务平台。如精准扶贫调查中，包括个性数据（姓名、家庭情况）和共性数据（交通通达度、人口密度、农田肥力、气象信息），支持位置匹配和信息推送。

---

## 本章总结

| 层次 | 要点 |
|------|------|
| 移动客户端 | 三种开发方式（Native/Web/Hybrid），WebView 混合编程机制 |
| 服务端 | 桥接移动端与数据端，TCP/HTTP 连接，Python/JSP 等实现 |
| 数据存储端 | 文件管理 + 数据库管理（Oracle/MySQL 等） |
| 应用分析端 | 时空维度分析：时序变化、空间格局、时空关联 |

> 课后实践：安装 Android SDK、VSCode、Node.js、Git；完成 H5 在线注册表单设计作业（用户名、密码、邮箱、电话，CSS控制输入框，JS校验非空）。