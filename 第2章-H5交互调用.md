# 第2章 H5和原生应用的交互调用

> [!SUMMARY] Table of Contents
>
> - [[第2章-H5交互调用#第2章 H5和原生应用的交互调用|第2章 H5和原生应用的交互调用]]
>   - [[第2章-H5交互调用#目录|目录]]
>   - [[第2章-H5交互调用#§1 基本原理|§1 基本原理]]
>     - [[第2章-H5交互调用#为什么需要交互|为什么需要交互]]
>     - [[第2章-H5交互调用#WebView 桥梁作用|WebView 桥梁作用]]
>     - [[第2章-H5交互调用#技术总览|技术总览]]
>   - [[第2章-H5交互调用#§2 JS 调用原生应用|§2 JS 调用原生应用]]
>     - [[第2章-H5交互调用#方法1 对象映射（addJavascriptInterface）|方法1 对象映射]]
>     - [[第2章-H5交互调用#方法2 URL 拦截（shouldOverrideUrlLoading）|方法2 URL 拦截]]
>     - [[第2章-H5交互调用#方法3 对话框拦截（WebChromeClient）|方法3 对话框拦截]]
>   - [[第2章-H5交互调用#§3 原生应用调用 JS|§3 原生应用调用 JS]]
>     - [[第2章-H5交互调用#WebView 初始化设置|WebView 初始化设置]]
>     - [[第2章-H5交互调用#调用方式|调用方式]]
>   - [[第2章-H5交互调用#流程总结|流程总结]]
>   - [[第2章-H5交互调用#环境与打包工具|环境与打包工具]]
>     - [[第2章-H5交互调用#Android 开发工具|Android 开发工具]]
>     - [[第2章-H5交互调用#APK 签名|APK 签名]]

---

## §1 基本原理

### 为什么需要交互

H5 应用基本能满足 APP 大多数需求，**一次编写，处处运行**。但部分需求（如调用摄像头、GPS 定位等）**必须用原生应用完成**。原生应用虽能访问任何资源，但不同平台需独立开发。

**混合开发思路**：不需原生功能的用 H5，必须用原生功能的用原生开发——这就需要 H5 与原生应用的交互。

```
H5 ←→ 原生应用 ←→ 本地资源
```

### WebView 桥梁作用

H5（HTML+CSS+JS）在界面设计方面具有跨平台优势；原生应用提供 **WebView / UIWebView / WKWebView**，作为粘合 H5 和原生功能的桥梁。

- **WebView** 是原生应用中的一个组件，用于加载和显示 H5 页面
- H5 通过 WebView 与原生交互，原生通过 WebView 调用 JS

### 技术总览

原生 App 实例化 WebView，加载 H5 页面。两种交互方向：

| 方向 | 方法 | 说明 |
|------|------|------|
| **原生调用 JS** | `loadUrl` 或 `evaluateJavascript` | 调用 H5 页面中的 JS 函数并传数据 |
| **JS 调用原生（3种）** | ① 暴露对象 | 原生 App 暴露对象给 JS 直接调用 |
| | ② URL 拦截 | 通过 `overrideUrl` 触发 `shouldOverrideUrlLoading` |
| | ③ 对话框拦截 | 通过 `alert/prompt/confirm` 触发 `WebChromeClient` 回调 |

---

## §2 JS 调用原生应用

### 方法1：对象映射（addJavascriptInterface）

通过 `WebView.addJavascriptInterface()` 将原生对象暴露给 JS：

**步骤**：
1. 编写原生接口类，用 `@JavascriptInterface` 标注
2. `webView.addJavascriptInterface(new JsInterface(this), "launcher")` 暴露对象
3. JS 中直接调用：`window.launcher.xxxMethod()`

### 方法2：URL 拦截（shouldOverrideUrlLoading）

通过 `WebViewClient` 的 `shouldOverrideUrlLoading()` 回调拦截 URL：

- JS 端构造特殊协议 URL（如 `js://methodName?params`）
- 原生端在 `shouldOverrideUrlLoading` 中解析 URL 并执行相应逻辑
- 建议制定应用级协议规范（如 `js://` 协议前缀），便于原生解析

### 方法3：对话框拦截（WebChromeClient）

通过 `WebChromeClient` 的 `onJsAlert()`、`onJsConfirm()`、`onJsPrompt()` 拦截 JS 对话框消息：

| JS 函数 | 原生回调 |
|---------|---------|
| `alert()` | `onJsAlert()` |
| `confirm()` | `onJsConfirm()` |
| `prompt()` | `onJsPrompt()` |

原生 App 拦截这些消息后执行相应业务逻辑。示例代码：`popwins.html` 中定义了 `callalert()`、`callprompt()`、`callconfirm()` 三个函数。

> H5 中元素通常有事件处理机制（如 button 的 `onclick`），可触发 JS 函数调用原生。

---

## §3 原生应用调用 JS

### WebView 初始化设置

```java
// 允许 WebView 加载 JS 代码
settings.setJavaScriptEnabled(true);
// 允许 JS 弹窗
settings.setJavaScriptCanOpenWindowsAutomatically(true);
// 暴露对象给 JS
webView.addJavascriptInterface(new JsInterface(this), "launcher");
// 加载 H5 页面
webView.loadUrl(url);
```

### 调用方式

| 方法 | 用法 | 说明 |
|------|------|------|
| `loadUrl()` | `webView.loadUrl("javascript:if(window.callJS){window.callJS('" + str + "');}")` | 通过加载 URL 方式执行 JS |
| `evaluateJavascript()` | 有版本依赖 | 更现代的方式，可获取返回值 |

两种方法都能调用 H5 页面中的 JS 函数并向 JS 传递数据。

---

## 流程总结

混合开发本质上以**原生应用为骨架嵌入 H5**，技术栈组合：

```
NPM + Angular + Ionic => H5（界面层）
Cordova => 原生插件（Android/iOS 桥接）

H5 + Cordova + 原生 => 跨平台移动应用 APP
```

关键规范：
- 原生应用必须提供 WebView，制定好 WebView 与 H5 的交互机制
- 应用级协议规范：如 JS 调用原生 API 时使用 `js://` 协议，便于原生解析
- H5 从原生获取信息（如空间定位）通过上述调用链：H5 → JS 调用原生 → 原生返回数据 → 原生调用 JS → H5 更新显示

---

## 环境与打包工具

### Android 开发工具

| 工具                    | 用途                                                                    |
| --------------------- | --------------------------------------------------------------------- |
| **Android SDK + JDK** | 基础开发环境                                                                |
| **Gradle**            | 构建工具 `gradlew build` / `assembleDebug` / `assembleRelease`            |
| **emulator**          | 虚拟机管理 `emulator -list-avds` / `-avd AVD_NAME`                         |
| **adb**               | 调试桥 `adb shell pm list packages -3` / `adb install` / `adb uninstall` |

常用 Gradle 命令：
- `gradlew build`——打包项目
- `gradlew assembleDebug`——构建 Debug 版 APK
- `gradlew assembleRelease`——构建 Release 未签名 APK
- `gradlew installDebug`——构建并在模拟器运行
- `gradlew uninstallDebug`——卸载已安装 Debug 版本
- `gradlew clean`——清理构建产物

### APK 签名

发布 APK 需**数字证书签名**，可通过 JDK 自带的 `keytool` 生成：

```bash
keytool -genkey -alias helloalias -keyalg RSA -keysize 2048 -validity 36500 -keystore hello.keystore
```

签名 APK（`apksigner` 位于 `build-tools` 目录下）：
```bash
apksigner sign --ks hello.keystore --out app-release-signed.apk app-release-unsigned.apk
```

验证签名：`apksigner verify -v /path/to/your.apk`

查看 debug.keystore 指纹：
```bash
keytool -list -v -keystore debug.keystore -alias androiddebugkey -storepass android -keypass android
```

> debug.keystore 默认位于 `C:\Users\<用户名>\.android` 或 Android SDK 的 `.android` 目录下，密码为 `android`。