# 第2章 移动应用客户端编程的包管理

> [!SUMMARY] Table of Contents
>
> - [[第2章-包管理#第2章 移动应用客户端编程的包管理|第2章 移动应用客户端编程的包管理]]
>   - [[第2章-包管理#目录|目录]]
>   - [[第2章-包管理#§1 深入 H5|§1 深入 H5]]
>     - [[第2章-包管理#HTML 基础回顾|HTML 基础回顾]]
>     - [[第2章-包管理#CSS 选择器|CSS 选择器]]
>     - [[第2章-包管理#JavaScript 函数|JavaScript 函数]]
>     - [[第2章-包管理#箭头函数|箭头函数]]
>     - [[第2章-包管理#回调函数|回调函数]]
>     - [[第2章-包管理#Promise 异步模式|Promise 异步模式]]
>   - [[第2章-包管理#§2 NPM|§2 NPM]]
>     - [[第2章-包管理#模块化与包|模块化与包]]
>     - [[第2章-包管理#Node.js 与 JS 模块规范|Node.js 与 JS 模块规范]]
>     - [[第2章-包管理#NPM 生态与 package.json|NPM 生态与 package.json]]
>     - [[第2章-包管理#scripts 脚本|scripts 脚本]]
>     - [[第2章-包管理#创建与发布 NPM 包|创建与发布 NPM 包]]
>     - [[第2章-包管理#npm link 本地测试|npm link 本地测试]]

---

## §1 深入 H5

### HTML 基础回顾

HTML（超文本标记语言）不是编程语言，而是**标记语言**，使用标记标签描述网页。HTML5 是最新标准，在元素和属性定义上有所修改，支持 CSS/JavaScript。

CSS 通过**选择器（selector）** 为特定元素设置样式（字体、颜色、背景、布局等）。JS 通过函数和事件实现动态交互，可进行计算、修改 DOM 结构、控制元素的 CSS。

### CSS 选择器

基本选择器包括：标签（元素）选择器、类选择器、ID选择器。此外还有对象选择器、CSS 组合选择器等。

**CSS 盒子模型**：`margin`（外边距）和 `padding`（内边距）是布局核心概念，可通过 `<div>` 配合 CSS 实现页面布局控制。

### JavaScript 函数

三种定义形式：

- **有名称函数（方法1）**：`var func_name = function(a, b) { return a + b; };`
- **有名称函数（方法2）**：`function func_name(a, b) { return a + b; };`
- **匿名函数**：`(function(a, b) { return a + b; })(10, 20);`

> 实践：编写 Web 计算器，输入 a、b，输出 sum 值，分别用三种方式实现。

### 箭头函数

ES6 引入箭头函数 `=>`，可写更短的函数：

| 场景 | 普通写法 | 箭头写法 |
|------|---------|---------|
| 普通函数 | `function hello(name) { return "Hello," + name; }` | `const hello = (name) => "Hello," + name;` |
| 数组回调 | `arr.map(function(x) { return x * 2; })` | `arr.map(x => x * 2)` |
| 匿名/定时器 | `setTimeout(function() { ... }, 1000)` | `setTimeout(() => { ... }, 1000)` |

**适用场景**：普通工具函数、数组回调、不依赖 `this` 的事件函数。
**不适用**：构造函数、对象方法中依赖 `this` 的函数、依赖 `arguments` 的函数。

### 回调函数

被作为参数传递给另一个函数的函数。JS 是单线程执行的脚本语言，遇到阻塞调用会造成整个线程阻塞，因此**异步处理**很常见，通过回调形式完成。

**同步回调**：调用后立即执行回调。  
**异步回调**：通过 `setTimeout` 等机制延迟执行回调。

### Promise 异步模式

Promise 将"生产者代码"和"消费者代码"连接在一起：

- **生产者**：`new Promise((resolve, reject) => { ... })`——执行异步任务，成功时 `resolve(value)`，失败时 `reject(error)`
- **消费者**：`promise.then(onFulfilled, onRejected).catch(handleError)`——接收结果并继续处理

支持链式调用：`promise.then(step1).then(step2).then(step3).catch(handleAll)`，实现多级加工处理。

> 实践：用回调函数和 Promise 两种方式读取并显示 JSON 文件内容。

---

## §2 NPM

### 模块化与包

程序越来越大，需要**模块化**。模块是代码，**包（package）是模块的集装箱**。典型包结构：

```
my-package/
├── package.json    // 包描述文件
├── index.js        // 主模块
├── lib/            // 其他模块
│   └── utils.js
└── node_modules/   // 依赖的其他包
```

### Node.js 与 JS 模块规范

Node.js 是让 JavaScript 能在服务器端运行的运行环境，NPM 能更好地支持模块化管理。

| 规范 | 说明 | 语法 |
|------|------|------|
| **CommonJS** | Node.js 早期常用 | `const x = require('./a'); module.exports = x;` |
| **AMD** | 早期浏览器端异步规范（RequireJS） | `define(['a'], function(a) { return a; });` |
| **UMD** | 兼容 AMD 和 CommonJS | 通用模块写法 |
| **ES6** | JS 标准模块化方案 | `import a from './a'; export default a;` |

Node.js 现已原生支持 `import/export`。注意区别：`module.exports` 导出对象，`exports.xx` 导出方法或变量（`exports` 是指向 `module.exports` 的指针，不能直接赋值给 `exports`）。

### NPM 生态与 package.json

NPM 是 JavaScript 共享生态，无数优秀的包由 NPM 管理。`package.json` 是包的描述文件，定义了依赖等关键信息。

`npm init -y` 初始化项目，生成 package.json，包含：
- `name`、`version`、`description`、`main`（入口文件）
- `type: "module"`（启用 ES6 模块）
- **`dependencies`**：生产环境依赖（运行时必须，如 react、jquery）
- **`devDependencies`**：开发环境依赖（开发测试所需，打包上线后无需）
- 修改属性：`npm pkg set name=test`

国内建议配置镜像加速：`npm config set registry https://registry.npmmirror.com`

### scripts 脚本

通过 `npm run script-key` 调用合并的脚本命令：

```json
"scripts": {
  "dev": "node build/dev-server.js",
  "build": "node build/build.js"
}
```

- `npm run` 不加参数可列出所有可执行脚本
- `npm test` = `npm run test`，`npm start` = `npm run start`
- `&&` 串行执行，`&` 并行执行
- **pre-/post- 钩子**：如 `pretest` → `test` → `posttest` 链式执行，出错则中止后续

### 创建与发布 NPM 包

以创建 `get-subtract-test` 包为例：

1. 创建目录，`npm init -y`
2. 编写 `index.js`（入口文件），用 `exports.getSubtract = function(x, y) { return x - y; }`
3. 编写 `test.js` 测试：`const myPackage = require('./index.js'); myPackage.getSubtract(8, 3);`
4. 发布前到 npmjs.com 检查包名是否冲突
5. `npm publish` 发布，更新版本修改 `version` 后重新 `npm publish`

**包目录结构约定**：`bin/`（二进制）、`lib/`（JS 代码）、`doc/`（文档）、`test/`（单元测试）。Node 对包结构要求不严格，只要顶层有 package.json 即可。

### npm link 本地测试

两个项目联动开发：

1. **npm-link-module**（待开发的 NPM 模块）：执行 `npm link`，链接到全局
2. **npm-link-example**（依赖模块的项目）：执行 `npm link npm-link-module`，全局包链接到本地 `node_modules/`

修改被链接库可实时查看变化。删除链接：`npm uninstall <package_name>`（局部）或 `npm uninstall <package_name> -g`（全局）。

> 课后作业1：用 div 封装注册页面，CSS 控制居中；用回调和 Promise 分别读取 JSON 注册信息文件。  
> 课后作业2：编写 NPM 包实现加/减/乘/除运算工具，完成本地测试。