# 第3章 时空信息移动编程的服务端设计与实现

> [!SUMMARY] Table of Contents
>
> - [[第3章-服务端#第3章 时空信息移动编程的服务端设计与实现|第3章 时空信息移动编程的服务端设计与实现]]
>   - [[第3章-服务端#§1 服务端实现的支撑环境|§1 服务端实现的支撑环境]]
>   - [[第3章-服务端#§2 服务端实现的主要编程工具|§2 服务端实现的主要编程工具]]
>   - [[第3章-服务端#§3 服务端实现的编程实践|§3 服务端实现的编程实践]]
>     - [[第3章-服务端#数据库端设计|数据库端设计]]
>     - [[第3章-服务端#后台服务实现（JSP）|后台服务实现（JSP）]]
>     - [[第3章-服务端#CORS 跨域处理|CORS 跨域处理]]
>     - [[第3章-服务端#前端服务层设计|前端服务层设计]]
>     - [[第3章-服务端#表单页面实现|表单页面实现]]
>     - [[第3章-服务端#前后端完整流程|前后端完整流程]]

---

## §1 服务端实现的支撑环境

服务端架构：

```
移动应用 ↔ 互联网 ↔ 云服务器
                      ├── JSP / NodeJS / Python / ASP / PHP
                      └── 云数据存储（文件存储 / 数据库存储）
```

**动态服务技术**：JSP、PHP、NodeJS、Python、ASP

**数据持久化**：文件存储、数据库存储

还需要注意**网络及服务端口设置**。

---

## §2 服务端实现的主要编程工具

| 类别 | 工具 |
|------|------|
| 编辑器/IDE | Eclipse、VS Code |
| 服务端语言 | JSP、PHP、NodeJS、Python |
| 数据库管理工具 | pgAdmin 等 |
| 服务端测试工具 | Postman |

---

## §3 服务端实现的编程实践

以采集土壤养分/类型信息的移动应用为例，整体分为数据库 → JSP 后台 → 移动端三层。

### 数据库端设计

使用 **PostgreSQL + PostGIS**。

**建表**：

```sql
-- 采集点位表
drop table if exists ydbc_sites;
create table ydbc_sites(
  id serial,
  c_time timestamptz not null,
  lat float not null,
  lon float not null,
  address varchar not null,
  c_data json not null,
  site_desc varchar
);

-- 类型字典表
drop table if exists ydbc_types;
create table ydbc_types(
  id serial,
  attr_name varchar
);
insert into ydbc_types(attr_name) values('土壤养分'), ('土壤类型');
```

**获取采样类型列表函数**：

```sql
create or replace function gettypes()
returns json language sql
as $$
  select json_build_object(
    'code', 200,
    'data', json_agg(row_to_json(x))
  )
  from (select * from ydbc_types) x;
$$;
```

**保存采集数据函数**：接收 JSON 参数，解析后插入 `ydbc_sites` 表，返回 `{"code": 200, "msg": "提交成功"}`。

**读取采集数据函数**：从 `ydbc_sites` 表查询所有记录，以 JSON 格式返回。

**统一分发函数 `dispatch_fun`**：根据传入的 JSON 参数中的 `fname` 字段，动态调用对应的数据库函数（如 `gettypes` 或 `savedata`），实现统一入口调度。

关键点：
- 如果 `fparam` 为空，调用无参函数
- 如果 `fparam` 不为空，将参数传入目标函数
- 通过函数返回的 `code` 判断是否成功

### 后台服务实现（JSP）

使用 JSP + JDBC 连接 PostgreSQL 数据库，为前端提供 RESTful API 接口。

**第一步：引入包和设置 CORS 头**

```jsp
<%@ page language="java" pageEncoding="UTF-8"
    contentType="application/json; charset=UTF-8" %>
<%@ page import="java.io.*" %>
<%@ page import="java.sql.*" %>
<%
  // CORS 跨域头设置
  response.setHeader("Access-Control-Allow-Origin", "*");
  response.setHeader("Access-Control-Allow-Methods", "POST, GET, OPTIONS");
  response.setHeader("Access-Control-Allow-Headers", "Content-Type, Authorization");
  response.setHeader("Access-Control-Max-Age", "3600");

  // OPTIONS 预检请求直接返回
  if ("OPTIONS".equalsIgnoreCase(request.getMethod())) {
    response.setStatus(HttpServletResponse.SC_OK);
    return;
  }

  request.setCharacterEncoding("UTF-8");
  response.setCharacterEncoding("UTF-8");
  response.setContentType("application/json; charset=UTF-8");
```

**第二步：读取请求的 JSON Payload**

通过 `request.getReader()` 逐行读取客户端 POST 提交的 JSON 数据到字符串 `payload`。

**第三步：数据库连接与 SQL 执行**

```java
String db_url = "jdbc:postgresql://localhost:5432/postgis_test";
Class.forName("org.postgresql.Driver");           // 加载驱动
Connection db_conn = DriverManager.getConnection(
    db_url, "postgres", "postgres");
String sql_text = "select dispatch_fun(?::json)"; // 调用分发函数
PreparedStatement stmt = db_conn.prepareStatement(sql_text);
stmt.setString(1, payload);
ResultSet result_set = stmt.executeQuery();
if (result_set.next()) {
  resp_str = result_set.getString(1);             // 直接输出数据库返回值
} else {
  resp_str = "{\"code\":500,\"msg\":\"数据库没有返回结果\"}";
}
```

**第四步：关闭资源**（finally 块中依次关闭 ResultSet、Statement、Connection）

### CORS 跨域处理

**CORS（跨域资源共享）** 是浏览器的安全机制，限制网页从不同源（域名、协议、端口）请求资源。

跨域请求流程：
1. 浏览器判断请求是否为 GET/POST/HEAD 且无自定义头
2. 如果不是，先发送 **OPTIONS 预检**请求
3. 服务端返回 CORS 响应头（`Access-Control-Allow-Origin` 等）
4. 通过后 → 发送实际请求；不通过 → 浏览器报错

**返回状态码规范**：

| 来源 | 状态码 | 含义 |
|------|--------|------|
| PostgreSQL 函数返回 | `code: 200` | 成功 |
| PostgreSQL 函数返回 | `code: 201` | 业务错误 |
| JSP 层面异常 | `code: 500` | 驱动加载失败 / SQL 异常 / 连接失败 / payload 读取失败 |

### 前端服务层设计

在 Angular/Ionic 应用中创建与后台交互的服务：

```bash
ionic generate service testprovider --skip-tests
```

服务实现：

```typescript
import { Injectable } from '@angular/core';

export interface ApiResponse {
  code: number;
  msg?: string;
  data?: any;
}

@Injectable({ providedIn: 'root' })
export class TestproviderService {
  private hostUrl = 'http://192.168.1.101:81/dataconnection.jsp';

  async getData(param: any): Promise<ApiResponse> {
    const response = await fetch(this.hostUrl, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(param)
    });
    const result: ApiResponse = await response.json();
    if (result.code !== 200) {
      throw new Error(result.msg || '请求失败，错误码：' + result.code);
    }
    return result;
  }
}
```

### 表单页面实现

在 `tab1.page.ts` 中：

- `ionViewWillEnter()` 时调用 `loadSiteTypes()` 获取采集类别列表（调用 `gettypes`）
- 保存：`this.siteTypes` 数组绑定到 `<select>` 下拉框

在 `tab1.html` 中收集以下字段：

- **属性名**（下拉选择类型）
- **属性值**（采集的具体值）
- **地址描述**（site_desc，如 "cropland"）
- **联系电话**（tel）
- **采集人**（c_person）
- **详细地址**（address）
- **经纬度**（lat, lon，由定位获取）

提交数据时调用 `savedata` 接口：

```typescript
async submit(): Promise<void> {
  try {
    const res = await this.httpservice.getData({
      fname: 'savedata',
      fparam: {
        attr_code: Number(this.attr_code),
        attr_value: this.sitedata.value,
        site_desc: this.site_desc,
        c_time: this.sitedata.time,
        c_person: this.c_person,
        tel: this.tel,
        address: this.address,
        lat: Number(this.sitedata.y_loc),
        lon: Number(this.sitedata.x_loc)
      }
    });
    this.msg = res.msg || '提交成功';
  } catch (err: any) {
    this.msg = err.message;
  }
}
```

### 前后端完整流程

| 步骤 | 操作 |
|------|------|
| 第一步 | 数据库准备：创建 `ydbc_sites` 表 → 创建 `savedata` 函数 → 创建 `dispatch_fun` 分发函数 |
| 第二步 | 准备 JSP 后台：修改连接字符串、用户名密码；用 Postman 测试 POST 接口 |
| 第三步 | 移动端输入：创建 `testprovider` 服务 → 修改 tab1 提交函数 |
| 第四步 | 移动端显示：通过 `getsites` 接口在 tab2 中显示已采集信息 |

**常见问题**：`Failed to load resource: net::ERR_CLEARTEXT_NOT_PERMITTED`

解决方法：
1. 修改 `config.xml`，在 `<application>` 标签添加 `android:usesCleartextTraffic="true"`
2. 修改 `resources/android/xml/network_security_config.xml`，为域名添加 `cleartextTrafficPermitted="true"`