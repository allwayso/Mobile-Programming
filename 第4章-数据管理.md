# 第4章 数据管理及应用分析

> [!SUMMARY] Table of Contents
>
> - [[第4章-数据管理#§1 数据管理与建模|§1 数据管理与建模]]
>   - [[第4章-数据管理#数据库平台|数据库平台]]
>   - [[第4章-数据管理#模拟应用—COVID 社交距离与感染率|模拟应用—COVID 社交距离与感染率]]
>   - [[第4章-数据管理#模拟数据生成|模拟数据生成]]
> - [[第4章-数据管理#§2 移动应用计算模型|§2 移动应用计算模型]]
>   - [[第4章-数据管理#移动数据轨迹分析|移动数据轨迹分析]]
>   - [[第4章-数据管理#SD 流通指数计算|SD 流通指数计算]]
>   - [[第4章-数据管理#属性相关性分析|属性相关性分析]]
>   - [[第4章-数据管理#时空交集分析|时空交集分析]]
>   - [[第4章-数据管理#K-Means 空间聚类|K-Means 空间聚类]]

---

## §1 数据管理与建模

### 数据库平台

移动数据的管理采用数据库方式，数据库丰富的函数为移动数据的管理和分析提供良好支持。

使用 **PostgreSQL + PostGIS** 作为移动数据管理的软件平台。

参考文档：https://postgis.net/docs/manual-3.2/

### 模拟应用—COVID 社交距离与感染率

**研究问题**：如何通过收集公众的移动数据，来研究移动强度（SD）是否和公共健康的感染指数 Infectious Rate（IR）成正相关？

**研究思路**：

1. 采集不同时间下人的坐标（所在城市）
2. 统计不同时间下的感染率数据
3. 计算公众移动情况
4. 根据移动情况建模计算 SD
5. 利用统计学，计算 SD 和 IR 之间的关系

**需要的表结构**：

| 表名 | 说明 |
|------|------|
| `users` | 人员表 |
| `admin_polygon` | 行政区域表（面状空间实体） |
| `user_locations` | 人员位置表（时空数据） |

参考：研究表明，在受新冠疫情影响最严重的县，人口流动趋势和新增确诊病例的增长速率密切相关（Origin–destination trip matrices at the US county level derived from aggregated and anonymised cell phone data）。

### 模拟数据生成

**创建行政区表**（15 个区，以武汉经纬度为基准生成网格）：

```sql
create table admin_polygon(id int, name varchar, geom Geometry);

insert into admin_polygon
select
  row_number() over() id,
  st_setsrid(st_makepolygon(st_makeline(array[
    st_setsrid(st_makepoint(114.17+0.1*(i-1), 30.54-0.1*(j-1)), 4326),
    st_setsrid(st_makepoint(114.17+0.1*i, 30.54-0.1*(j-1)), 4326),
    st_setsrid(st_makepoint(114.17+0.1*i, 30.54-0.1*(j-2)), 4326),
    st_setsrid(st_makepoint(114.17+0.1*(i-1), 30.54-0.1*(j-2)), 4326),
    st_setsrid(st_makepoint(114.17+0.1*(i-1), 30.54-0.1*(j-1)), 4326)
  ])), 4326) geom
from (select i, j from generate_series(1,3) j, generate_series(1,5) i) x;
```

> 从 (114.17, 30.54) 开始，按 0.1 间隔划分 3×5=15 个网格区域。

**创建用户表**（150 个用户）：

```sql
create table users(id int, otherinfo varchar);
insert into users select id, 'user#' || id from generate_series(1, 150) id;
```

**创建位置表并初始化**（每个区 10 个人）：

```sql
create table user_locations(
  id serial, user_id int, geom Geometry, t timestamptz
);

insert into user_locations(user_id, geom, t)
select row_number() over() usr_id,
  st_generatepoints(geom, 1) geom, now()
from generate_series(1,10), admin_polygon;
```

> `st_generatepoints` 在每个多边形内随机生成 1 个点，10 轮后每个区平均分配 10 个点，共 150 个点。

**模拟每日移动（以 t1 天为例）**：

用户随机移动，最大距离 0.1，若仍在 `admin_polygon` 内则更新位置；未移动的点则复制前一天的位置（打上新的时间戳）。

```sql
-- 移动的点
insert into user_locations(user_id, geom, t)
select user_id, new_geom, now() + interval '1 day'
from user_locations,
     ST_Translate(geom, 0.1*random(), 0.1*random()) new_geom
where exists (
  select from admin_polygon where st_contains(geom, new_geom)
);

-- 未移动的点
insert into user_locations(user_id, geom, t)
select user_id, geom, now() + interval '1 day'
from user_locations
where user_id not in (
  select user_id from user_locations
  where t::date = (now() + interval '1 day')::date
);
```

> t2 天在 t1 基础上类推，每次间距为 1 day。

---

## §2 移动应用计算模型

### 移动数据轨迹分析

移动数据轨迹 = **空间 + 时间**。

分析维度包括：

| 维度 | 说明 |
|------|------|
| 内部流通指数 | 统计某个区域内的人员流动情况 |
| 进出指数 | 统计不同区域之间的人员流入/流出 |
| 相关性 | 通过某个属性指标，计算内部流通和进出指数的相关性 |
| 时空交集 | 两个移动用户（设备）在空间和时间上是否有交集 |
| 空间聚集模式 | 使用 K-Means 分类分析空间聚集模式 |

### SD 流通指数计算

**计算某天每个用户所在区域**：

```sql
select u.user_id, adm.id
from user_locations u
left join admin_polygon adm on st_contains(adm.geom, u.geom)
where t::date = now()::date
order by user_id;
```

**计算 t0→t1 天内发生跨区域移动的用户**：

```sql
select t0.user_id, adm_t0, adm_t1
from (
  select u.user_id, adm.id adm_t0
  from user_locations u join admin_polygon adm on st_contains(adm.geom, u.geom)
  where t::date = now()::date
) t0
join (
  select u.user_id, adm.id adm_t1
  from user_locations u left join admin_polygon adm on st_contains(adm.geom, u.geom)
  where t::date = (now() + interval '1 day')::date
) t1 on t0.user_id = t1.user_id
where adm_t0 <> adm_t1;
```

> t1→t2 同理，日期区间改为 `(now()+interval '1 day')` 到 `(now()+interval '2 day')`。

通过 `adm_t0` 与 `adm_t1` 不同区域的对比，统计每个 `admin_polygon` 在不同时间段的进/出人数，即**流通指数（SD）**。

### 属性相关性分析

以 COVID 为例，若每个移动轨迹 ID 记录了是否阳性，可以分析阳性率与流通指数的相关性。

**COVID 测试表**：

```sql
create table covid(id int, covidtest boolean, t date);
insert into covid(id, covidtest, t) values
  (1, true, '2022-01-01'),
  (2, false, '2022-01-01'),
  (3, false, '2022-01-01');
```

使用 PostgreSQL 的 `corr(x, y)` 函数计算皮尔逊相关系数：

```sql
select corr(x, y) from (
  select * from (values(1,2),(3,4),(5,6)) as st(x, y)
) x;
```

分析流程：
1. 计算不同区域 ID 内 COVID 为 true 的比例
2. 计算该区域的流通指数（内部/外部/总和）
3. 用 `corr` 计算两者的相关性

### 时空交集分析

问题：如何判断两个移动轨迹在**空间和时间上**是否有交集？扩展到 n 个人的情况？

核心是：匹配两个用户的轨迹在相近时间点、相近空间位置上的重合情况，可利用 PostGIS 的时空查询函数进行叠加分析。

### K-Means 空间聚类

对 `user_locations` 中的位置点进行空间聚类：

```sql
select ID, ST_ClusterKMeans(geom, 3) OVER() CID, geom
from user_locations;
```

> `ST_ClusterKMeans` 是 PostGIS 中的 K-Means 空间聚类函数，参数 3 表示分为 3 类，`CID` 为聚类结果的分组 ID。